import { RegresarDirective } from './regresar.directive';

describe('RegresarDirective', () => {
  it('should create an instance', () => {
    const directive = new RegresarDirective();
    expect(directive).toBeTruthy();
  });
});
