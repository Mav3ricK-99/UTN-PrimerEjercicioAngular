import { ContraseniaDebeConfirmarseDirective } from './contrasenia-debe-confirmarse.directive';

describe('ContraseniaDebeConfirmarseDirective', () => {
  it('should create an instance', () => {
    const directive = new ContraseniaDebeConfirmarseDirective();
    expect(directive).toBeTruthy();
  });
});
